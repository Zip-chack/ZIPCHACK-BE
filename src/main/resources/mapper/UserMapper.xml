<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Minyou.MINYOU.mapper.UserMapper">
    
    <resultMap id="UserResultMap" type="com.Minyou.MINYOU.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="name" column="name"/>
        <result property="username" column="username"/>
        <result property="resetToken" column="reset_token"/>
        <result property="resetTokenExpiry" column="reset_token_expiry"/>
        <result property="emailVerified" column="email_verified"/>
        <result property="emailVerificationCode" column="email_verification_code"/>
        <result property="emailVerificationCodeExpiry" column="email_verification_code_expiry"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    
    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users
    </select>
    
    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>
    
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>
    
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>
    
    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>
    
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>
    
    <select id="findByResetToken" resultMap="UserResultMap">
        SELECT * FROM users WHERE reset_token = #{resetToken}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, password, nickname, name, username,
            reset_token, reset_token_expiry,
            email_verified, email_verification_code, email_verification_code_expiry,
            created_at
        ) VALUES (
            #{email}, #{password}, #{nickname}, #{name}, #{username},
            #{resetToken}, #{resetTokenExpiry},
            #{emailVerified}, #{emailVerificationCode}, #{emailVerificationCodeExpiry},
            NOW()
        )
    </insert>
    
    <update id="update">
        UPDATE users SET
            email = #{email},
            password = #{password},
            nickname = #{nickname},
            name = #{name},
            username = #{username},
            reset_token = #{resetToken},
            reset_token_expiry = #{resetTokenExpiry},
            email_verified = #{emailVerified},
            email_verification_code = #{emailVerificationCode},
            email_verification_code_expiry = #{emailVerificationCodeExpiry}
        WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM users WHERE id = #{id}
    </delete>
    
</mapper>

